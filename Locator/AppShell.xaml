<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Locator.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Locator"
    Title="Locator">

    <!--ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" /-->
    <FlyoutItem Title="First" >
        <ShellContent Title="Home" ContentTemplate="{DataTemplate local:FirstPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Second" >
        <ShellContent Title="Settings" ContentTemplate="{DataTemplate local:SecondPage}" />
    </FlyoutItem>

</Shell>
